## 요청 처리 과정

dispatch servlet 을 기억해라

1. filter로 보안, 인코딩 실행
2. dispatcher servlet에게 위임
3. handlermapping이 알맞는 컨트롤러를 찾는다.
4. 인터셉터 실행 (springSecurity같은 PreHandle)
4. HandlerAdapter에게 컨트롤러 실행해달라고 요청한다.
   5. Argument Resolver가 @RequestBody 등의 파라미터 처리
5. 컨트롤러가 실행한다.
6. HandlerAdapter가 응답 처리